import 'package:flutter/material.dart';


class Call extends StatelessWidget {
  static String routname = "Call";
  const Call({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Call"),
      ),
    );
  }
}
